#include<stdio.h>
int main()
{
  int x;
  printf("enter the number : ");
  scanf("%d",&x);
  if(x>99)
  {
    printf("no. is three digit");
  }
  else
  {
  printf("no. is not three digit");
  }
  return 0;
}